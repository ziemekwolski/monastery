.category.row
  %section
    - categories.each do |category|
      .category-preview.col-xs-6.col-sm-4
        %h2= category.name
        = link_to category_path(category) do
          %img{:alt => "category-image", :src => category.upload.try(:file).try(:url , :vertical_620)}/