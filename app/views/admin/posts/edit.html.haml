.row
  .col-lg-12
    %h1.page-header
      = t('admin.editing', name: @post.title)
      .pull-right
        = link_to t('admin.view'), post_path(@post), class: "btn btn-default", target: "_blank"

= render 'form'